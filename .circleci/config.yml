 version: 2
 jobs:
   test:
     docker:
       - image: circleci/ruby:2.3.0-jessie-node
         environment:
           PGHOST: 127.0.0.1
           PGUSER: postgres
           RAILS_ENV: test
       - image: circleci/postgres:9.5-alpine
         environment:
           POSTGRES_USER: postgres
           POSTGRES_DB: myapp_test
           POSTGRES_PASSWORD: ""
     steps:
       - checkout
       - run:
          name: Which bundler?
          command: bundle -b
